# Deferred for the current phase.
# Local development runs directly with `go run ./src/cmd/api`.
#
# version: "3.9"
#
# services:
#   api:
#     build:
#       context: .
#     ports:
#       - "8080:8080"
#     environment:
#       LISTEN_ADDR: ":8080"
#     volumes:
#       - ./:/app
